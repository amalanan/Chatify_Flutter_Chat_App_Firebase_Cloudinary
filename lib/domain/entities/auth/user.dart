class UserEntity {

  String ? name;
  String ? email;
  String ? image;
  DateTime ? lastActive;
  String ? uid;

  UserEntity({
    this.name,
    this.email,
    this.image,
    this.lastActive,
    this.uid,
  });
}